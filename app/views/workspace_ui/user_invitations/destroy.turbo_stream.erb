<%= turbo_stream.replace 'user_invitations_frame' do %>
  <turbo-frame id="user_invitations_frame">
    <%= render 'workspace_ui/user_invitations/index' %>
  </turbo-frame>
<% end %>

<%= turbo_stream.replace 'notification_frame' do %>
  <turbo-frame id="notification_frame">
    <%= ui_kit :notification, flash: flash %>
  </turbo-frame>
<% end %>
